{
    "query": "mutation Login($username: String!, $password: String!) { login(username: $username, password: $password) { message }}",
    "variables": { 
        "username": "taina", 
        "password": "123456" 
    }
}